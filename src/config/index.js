import dotenv from "dotenv";
dotenv.config();

const {
  PORT,
  DB_USER,
  DB_HOST,
  DB_NAME,
  DB_PASSWORD,
  DB_PORT,
  JWT_SECRET,
  NODE_ENV,
  TEST_SERVER,
  SESSION_SECRET,
  CLIENT_URL,
  GMAIL_USERNAME,
  GMAIL_APP_PASSWORD,
} = process.env;

export {
  PORT,
  DB_USER,
  DB_HOST,
  DB_NAME,
  DB_PASSWORD,
  DB_PORT,
  JWT_SECRET,
  NODE_ENV,
  TEST_SERVER as testServer,
  SESSION_SECRET,
  CLIENT_URL,
  GMAIL_USERNAME,
  GMAIL_APP_PASSWORD,
};
